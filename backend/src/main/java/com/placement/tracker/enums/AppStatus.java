package com.placement.tracker.enums;

// This enum tracks the progress of a student's application through each recruitment stage.
public enum AppStatus {
    APPLIED,
    ELIGIBLE,
    SHORTLISTED_OA,
    OA_CLEARED,
    INTERVIEW_SCHEDULED,
    OFFERED,
    REJECTED
}
