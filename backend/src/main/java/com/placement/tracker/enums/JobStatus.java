package com.placement.tracker.enums;

// This enum represents the lifecycle state of a job post from opening to final results.
public enum JobStatus {
    OPEN,
    CLOSED,
    ONGOING,
    RESULTS_OUT
}
